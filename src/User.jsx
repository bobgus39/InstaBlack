import SearchInput from "./SearchInput";

function User() {
  return (
    <>
      /*
      {userPosts ? (
        <ul>
          {userPosts.data.map((user) => (
            <li key={user.id}>
              <h2>{user.username}</h2>
            </li>
          ))}
        </ul>
      ) : (
        0
      )}
      */
    </>
  );
}

export default User;
